server:
  port: 8090

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/springweb?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.jj.spb.spring_boot_mybatis
  configuration:
    database-id: springweb

#showsql
logging:
  level:
    com.jj.spb.spring_boot_mybatis:
      mapper:debug